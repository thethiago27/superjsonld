# superjsonld

## 0.2.0

### Minor Changes

- 680d69f: Initial release: configurable JSON-LD (schema.org) builders with a `createJsonLd` factory, standalone builders typed via `schema-dts`, and an optional `superjsonld/react` `<JsonLd>` component.
