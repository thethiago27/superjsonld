---
"superjsonld": minor
---

Initial release: configurable JSON-LD (schema.org) builders with a `createJsonLd` factory, standalone builders typed via `schema-dts`, and an optional `superjsonld/react` `<JsonLd>` component.
